//stdin  leitura console
//stdout escrita console
//stderr escrita no console (erro)

/*void abrirArquivo(const char *nomeArquivo) {
    arquivo = fopen(nomeArquivo, "r+");
    if (arquivo == NULL) {
        fprintf(stderr, "Erro ao abrir o arquivo.\n");
        exit(1); ou return;
    }
}

void fecharArquivo() {
    if (arquivo != NULL) {
        fclose(arquivo);
    }
}

*/